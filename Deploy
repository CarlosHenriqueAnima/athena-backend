pipeline {
    agent any

    stages {
        stage ('Deploy') {
            steps {
                script {
                   sh '/var/infra/deploy/athena-back/deploy.sh'
                }                
            }
        }
    }
}
